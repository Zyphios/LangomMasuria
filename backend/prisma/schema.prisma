// Stub schema for Task 2 - will be replaced in Task 3
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Stub {
  id Int @id @default(autoincrement())
}
